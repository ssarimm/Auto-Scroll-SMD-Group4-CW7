name: auto_scroll_demo
description: A Flutter package for creating auto-scrolling lists with granular speed control and pause-on-hold functionality.
version: 1.0.0
homepage: https://github.com/your-username/auto_scroll_demo # Add your GitHub link here

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true